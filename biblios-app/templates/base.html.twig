<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
        <title>Biblios - {% block title %}{% endblock %}</title>
        <link rel="icon" type="image/svg" href="{{ asset('images/library-icon.svg') }}">
        
        {% block meta_description %}
            <meta name="description" content="Biblios est un site expérimental de catalogue de bibliothèque en ligne pour la commune fictive de Trifouillis-les-Oies. Consultation, commentaires et gestion d’ouvrages générés automatiquement." />
        {% endblock %}

        <meta name="author" content="Maëlle Nioche" />
        
        {% block canonical %}
            <link rel="canonical" href="https://green-pony-128403.hostingersite.com/" />
        {% endblock %}

         <!-- SEO -->
         {% block meta_keywords %}
            <meta name="keywords" content="bibliothèque, catalogue, livres, consultation en ligne, site expérimental, Trifouillis-les-Oies, livres fictifs, commentaires, gestion d’ouvrages, Symfony, démonstration" />
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body id="page-top">
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top" id="mainNav">
            <div class="container px-4">
                <a class="navbar-brand" href="{{ path('app_main')}}">Biblios</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
                <div class="collapse navbar-collapse" id="navbarResponsive">
                    <ul class="navbar-nav ms-auto">
                        <li class="nav-item"><a class="nav-link" href="{{ path('app_book_index')}}">Catalogue</a></li>
                        {% if app.user %}
                            <li class="nav-item"><a class="nav-link" href="{{ path('app_logout') }}">Se déconnecter</a></li>
                        {% else %}
                            <li class="nav-item"><a class="nav-link" href="{{ path('app_login') }}">Se connecter</a></li>
                        {% endif %}
                    </ul>
                </div>
            </div>
        </nav>
        {% block body %}{% endblock %}
        <footer class="py-5 bg-dark text-white">
            <div class="container px-4 text-center">
                <p class="mb-2">Biblios</p>
                <p class="mb-2">
                    Développé par
                        <a href="https://github.com/" class="text-white">
                            Kōji</a> &copy; 2025
                </p>
                <p class="mb-2">
                    Ce site est open source et ne récolte aucune donnée. 
                    <a href="https://github.com/" class="text-white">
                        Consultez le code source sur GitHub
                    </a>.
                </p>
                <p class="mb-0 fst-italic">Ce site est une simulation.</p>
            </div>
        </footer>
    </body>
</html>